# Research log

The single narrative doc. Every number here is real output from `experiments/nbody_benchmark.py`
(full config + per-run results are persisted to `runs/nbody_benchmark.json`). Retractions are reported
in place, loudly, naming the check that failed.

---

No results recorded yet.
